[pytest]
addopts = --alluredir=reports/allure-results --clean-alluredir
pythonpath = .
markers =
    sanity:     Core happy-path tests — run on every deployment
    regression: Full suite including edge cases and DB validation
    auth:       Authentication and authorization tests
    get_api:    GET Users API tests
    post_api:   POST Users API tests
